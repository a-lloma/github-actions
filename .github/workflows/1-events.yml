name: '1 - events' 

on: 
    push:
        branches:
            - "feature/**"

jobs:
    build:
        runs-on: "ubuntu-latest"
        steps:
            - run: echo '---- ❤️primeiro teste'
              shell: bash